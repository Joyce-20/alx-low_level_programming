#include <stdio.h>
/**
 * main - a program that prints its name
 * @argc:is command count
 * @argv:is command value
 * Return:0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
